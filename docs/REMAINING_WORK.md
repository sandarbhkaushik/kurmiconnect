# KurmiConnect — Remaining Work Plan

> **How to use this doc:**
> Each section below is a self-contained Claude Code session. When you're ready to work on one, copy the entire prompt block for that session (everything between the opening ``` and closing ```), paste it into your Claude Code terminal, and let it work step-by-step with your approval.
>
> Do ONE session at a time. Don't paste multiple sessions in one message. Don't let Claude Code run agents for these — they must run in the main chat with approval between steps.
>
> After finishing a session, come back here and check the ✅ box for that session.

---

## Current status snapshot (as of foundation completion)

**Done:**
- ✅ Monorepo scaffold (apps/mobile, apps/admin, apps/api placeholders)
- ✅ Expo SDK 54 + React Native 0.81 + React 19
- ✅ NativeWind v4 configured with pnpm hoisting fix
- ✅ 79 mobile screen files generated by agent
- ✅ Custom bottom tab bar component
- ✅ Theme system in lib/theme.ts (108 lines)
- ✅ Component library (KCButton, MatchCard, Avatar, Badge, Field, etc.)
- ✅ Splash → Language navigation wired
- ✅ Firebase config planned (not yet installed)
- ✅ TypeScript compiles clean (0 errors)
- ✅ App runs on Android device
- ✅ Git tags: agent-generated-baseline, pre-sdk54-upgrade, foundation-ready, first-screen-on-phone

**Not done (this document covers all of this):**
- ⏳ Auth flow button wiring (language, phone, OTP, for-whom)
- ⏳ Design tokens ported to tailwind.config.js
- ⏳ i18n locale files (hi.json, en.json)
- ⏳ lib/ utilities (api.ts, auth.ts, storage.ts)
- ⏳ Firebase native modules installed
- ⏳ Remaining onboarding screens (basic, physical, community, location, native, education, profession, lifestyle, family, horoscope, about, photos, prefs)
- ⏳ Home + discovery screens wiring
- ⏳ Interest/chat/subscription screens wiring
- ⏳ FastAPI backend (all 13 modules)
- ⏳ Admin panel (24 screens)
- ⏳ Backend deployment to Hetzner
- ⏳ Firebase project setup + real OTP
- ⏳ Razorpay integration
- ⏳ Featured Personality workflow

---

## Session tracker

Frontend foundation (mobile):
- [x] Session 1 — Reusable BottomCTA + wire auth flow buttons
- [x] Session 2 — Port design tokens from tokens.jsx to tailwind.config.js
- [x] Session 3 — Set up i18n (hi.json, en.json, i18n.ts)
- [x] Session 4 — Build lib/api.ts, lib/storage.ts (Firebase auth deferred)

Backend foundation (API):
- [x] Session 5 — FastAPI scaffold + docker-compose + Tenant module
- [x] Session 6 — Auth module (password-based; Firebase verify + JWT deferred)
- [x] Session 7 — Profile module + endpoints
- [x] Session 8 — Match module + daily suggestion algorithm

Mobile → Backend wiring:
- [x] Session 9 — Wire onboarding screens 1-7 to Profile API
- [ ] Session 10 — Wire onboarding screens 8-14 + photo upload
      (screens 8-14 minus photos.tsx are done; photos.tsx + R2 upload endpoint
      still pending — needs Cloudflare R2 bucket + credentials in apps/api/.env first)
- [ ] Session 11 — Wire Home + Search + Profile detail

Communication features:
- [ ] Session 12 — Interest module (backend + wire mobile)
- [ ] Session 13 — Chat module (WebSocket backend + mobile chat screen)

Monetization:
- [ ] Session 14 — Subscription/Plans module + Razorpay integration
- [ ] Session 15 — Verification module (Aadhaar/education/employer)

Featured Personality (your USP):
- [ ] Session 16 — Featured Personality backend module
- [ ] Session 17 — Featured screens wiring on mobile

Admin panel:
- [ ] Session 18 — Admin scaffold + Login + Dashboard
- [ ] Session 19 — Admin: User management + Moderation
- [ ] Session 20 — Admin: Featured Personality editorial workflow
- [ ] Session 21 — Admin: Payments + Broadcasts + CMS

Launch prep:
- [ ] Session 22 — Firebase real OTP (dev build via EAS)
- [ ] Session 23 — Backend deploy to Hetzner + Cloudflare
- [ ] Session 24 — End-to-end smoke test + fix list

---

## Session 1 — Reusable BottomCTA + wire auth flow buttons

**Goal:** Add a "Continue" button to language, phone, OTP, and for-whom screens.
**Estimated time:** 45-60 minutes
**Blockers cleared after:** Complete auth flow navigable end-to-end.

```
Session 1: Add reusable BottomCTA component and wire it into the auth/onboarding 
screens that are missing Continue buttons.

Rules:
- Follow CLAUDE.md (no agents, one step at a time, wait for approval between steps)
- Show diffs before writing files
- Match existing KCButton styling — same saffron color, same 48px height
- Use expo-router's useRouter for navigation
- Preserve all existing visual elements — do NOT change layout or add features

STEP 1: Read these files and report what you find:
1. apps/mobile/components/ui/KCButton.tsx (existing button, likely reusable)
2. apps/mobile/lib/theme.ts (color tokens)
3. Last 30 lines each of:
   - apps/mobile/app/(auth)/language.tsx
   - apps/mobile/app/(auth)/phone.tsx
   - apps/mobile/app/(auth)/otp.tsx
   - apps/mobile/app/(onboard)/for-whom.tsx
4. Any existing router.push/replace calls in these four files

Report findings. Do NOT modify anything. Wait for "next".

STEP 2: Decide whether to:
(a) Reuse KCButton as-is with a new BottomCTA wrapper (recommended), OR
(b) Extend KCButton to support a bottom-pinned variant, OR  
(c) Create a fresh BottomCTA component

Propose your choice with reasoning. Wait for my approval.

STEP 3: Create/modify the button component per approved choice. Show diff.

STEP 4: Wire language.tsx:
- Add BottomCTA at bottom of screen: "जारी रखें / Continue"
- onPress: save selected language to MMKV (skip if lib/storage.ts doesn't exist yet — 
  use React state only, TODO comment for MMKV integration)
- Then router.push('/(auth)/phone')
- Button disabled until a language is selected (Hindi is pre-selected, so enabled by 
  default)
Show diff. Wait for approval.

STEP 5: Wire phone.tsx:
- Add BottomCTA: "OTP भेजें / Send OTP"  
- Disabled until: (a) valid 10-digit Indian mobile (starts with 6-9), 
  (b) Terms checkbox checked
- onPress: navigate to /(auth)/otp with phone param
- For now no actual Firebase call — just navigate with phone in params
Show diff. Wait for approval.

STEP 6: Wire otp.tsx:
- Add BottomCTA: "Verify और आगे बढ़ें / Verify & Continue"
- Disabled until all 6 digits entered
- onPress: navigate to /(onboard)/for-whom
- No real OTP verification yet — accept any 6 digits (TODO: real Firebase)
Show diff. Wait for approval.

STEP 7: Wire for-whom.tsx:
- Add BottomCTA: "आगे / Next"
- Disabled until an option is selected (Myself is pre-selected, so enabled by default)
- onPress: navigate to /(onboard)/basic
Show diff. Wait for approval.

STEP 8: Verify:
- pnpm tsc --noEmit (should be 0 errors)
- Report

STEP 9: Commit as: "feat(mobile): add BottomCTA and wire auth flow navigation"

After I confirm the four screens work end-to-end on my phone, tag as: session-1-done
```

**After session 1:** Test on your phone. Splash → Language → Phone → OTP → For-whom. Every screen should have a visible Continue button. Report back with any screens where navigation still fails.

---

## Session 2 — Port design tokens to Tailwind config

**Goal:** Extract colors, spacing, fonts from design/html-mockups/tokens.jsx into apps/mobile/tailwind.config.js so NativeWind classes match your brand.
**Estimated time:** 45-60 minutes
**Blockers cleared after:** Screens can use branded classes like `bg-saffron-600 text-ivory-50`.

```
Session 2: Port design tokens from design/html-mockups/tokens.jsx into 
apps/mobile/tailwind.config.js so all screens can use branded NativeWind classes.

Rules from CLAUDE.md apply. No agents. One step at a time.

STEP 1: Read and report:
1. design/html-mockups/tokens.jsx — full contents
2. apps/mobile/lib/theme.ts — current theme values
3. apps/mobile/tailwind.config.js — current config (probably minimal)

Report:
- What color families exist in tokens.jsx (saffron/amber, ivory/cream, etc.)?
- What are the exact hex values used?
- What font families?
- What spacing scale (if any)?
- Any semantic names (primary, success, error, etc.)?

Wait for "next".

STEP 2: Propose the updated tailwind.config.js as a diff. It should:
- Extend theme.colors with named color scales (e.g., saffron: {50, 100, 200, ...900})
- Extend theme.fontFamily with 'mukta', 'inter' etc.
- Extend theme.spacing only if tokens.jsx defines custom spacing
- Keep the existing NativeWind preset intact
- Content globs unchanged (./app/**/*, ./components/**/*)

Show the diff. Do not write yet. Wait for approval.

STEP 3: After approval, write tailwind.config.js.

STEP 4: Also update lib/theme.ts so it references the same values (or update it to 
import from a shared source of truth if that's cleaner). Show diff. Wait for approval.

STEP 5: Convert ONE screen as a pilot to use the new tokens. Recommendation: 
apps/mobile/app/index.tsx (splash) — smallest, safest.
- Replace hardcoded colors with className tokens (bg-cream-50, text-saffron-800, etc.)
- Show diff. Wait for approval.

STEP 6: Verify:
- pnpm tsc --noEmit
- I'll test on phone that splash still looks correct

STEP 7: Commit as: "feat(mobile): port design tokens to tailwind and theme.ts"

DO NOT convert other screens in this session — that's a bulk migration for later.
We just need the tokens available for future work.
```

**After session 2:** Splash should look identical (proves tokens map correctly). Other screens still use inline styles from theme.ts — that's fine, they'll migrate later.

---

## Session 3 — Set up i18n (Hindi + English)

**Goal:** Create locale files and wire i18next so screens can use `t('key')` for bilingual text.
**Estimated time:** 60-90 minutes  
**Blockers cleared after:** New screens can be built with proper i18n from day 1.

```
Session 3: Set up i18n for Hindi and English so screens can use useTranslation().

Rules from CLAUDE.md. No agents. Sequential with approvals.

STEP 1: Verify what's installed:
1. grep -E "i18next|react-i18next|expo-localization" apps/mobile/package.json
2. If not installed, we'll install; if installed, we'll wire them up.

Report. Wait for "next".

STEP 2: If needed, install packages using expo install for compatibility:
- npx expo install expo-localization
- pnpm add i18next react-i18next intl-pluralrules

Show install output. Wait for "next".

STEP 3: Create the locale files with real content for the auth flow screens 
(the ones already visible on phone). Structure:

apps/mobile/locales/hi.json:
{
  "common": {
    "continue": "जारी रखें",
    "next": "आगे",
    "back": "पीछे",
    "cancel": "रद्द करें",
    ...
  },
  "splash": {
    "brand": "KurmiConnect",
    "est": "EST. 2026",
    "tagline_line1": "विश्वसनीय रिश्ते,",
    "tagline_line2": "सम्मानित परिवार",
    "tagline_en": "Trusted matches, respected families"
  },
  "language": {
    "title": "अपनी भाषा चुनें",
    "subtitle": "Choose your language",
    "hindi": "हिन्दी",
    "english": "English",
    "marathi": "मराठी",
    "marathi_phase": "Phase 2",
    "coming_soon": "Coming soon"
  },
  "phone": {
    "title": "अपना मोबाइल नंबर डालें",
    "subtitle": "Enter your mobile number",
    "otp_hint": "OTP WhatsApp या SMS पर आएगा",
    "otp_hint_en": "OTP will arrive on WhatsApp or SMS",
    "terms_agree": "मैं Terms और Privacy Policy से सहमत हूँ",
    "terms_agree_en": "I agree to Terms and Privacy",
    "send_otp": "OTP भेजें",
    "already_login": "पहले से account है? Login"
  },
  "otp": {
    "title": "OTP डालें",
    "subtitle": "Enter OTP",
    "sent_to": "पर भेजा गया",
    "resend_in": "में दोबारा भेजें",
    "resend_sms": "OTP दोबारा भेजें",
    "resend_whatsapp": "WhatsApp पर भेजें",
    "resend_voice": "Voice call OTP",
    "verify": "Verify और आगे बढ़ें"
  },
  "onboard": {
    "for_whom": {
      "title": "यह profile किसके लिए है?",
      "subtitle": "Whose profile is this?",
      "help": "हम accordingly आपको दिखाएँगे · We'll personalize accordingly",
      "myself": "अपने लिए",
      "myself_en": "Myself",
      "son": "बेटे के लिए",
      "son_en": "My son",
      "daughter": "बेटी के लिए",
      "daughter_en": "My daughter",
      "brother": "भाई के लिए",
      "brother_en": "My brother",
      "sister": "बहन के लिए",
      "sister_en": "My sister",
      "relative": "रिश्तेदार",
      "relative_en": "Relative"
    }
  }
}

apps/mobile/locales/en.json:
Same structure but all values in English. For fields marked with "_en" in Hindi, 
those keys don't exist in en.json (they're for the bilingual display in Hindi UI).

Show me the full hi.json and en.json files. Wait for approval.

STEP 4: Create apps/mobile/lib/i18n.ts:
- Initialize i18next with hi as default
- resources loaded from ../locales/hi.json and ../locales/en.json
- fallbackLng: 'en'
- interpolation.escapeValue: false
- Read expo-localization getLocales() to detect device language (use en if not hi)
Show file. Wait for approval.

STEP 5: Wire i18n into apps/mobile/app/_layout.tsx:
- Import '../lib/i18n' at the top (side-effect import)
- Verify existing font loading still works
Show diff. Wait for approval.

STEP 6: Convert ONE screen to use useTranslation as pilot. 
Recommendation: apps/mobile/app/(auth)/language.tsx.
- Import useTranslation from 'react-i18next'
- Replace hardcoded strings with t('language.title'), t('language.subtitle'), etc.
- Preserve visual layout completely
Show diff. Wait for approval.

STEP 7: Verify:
- pnpm tsc --noEmit
- I'll test on phone that language.tsx still renders correctly in Hindi

STEP 8: Commit as: "feat(mobile): add i18n with hi and en locales"

DO NOT migrate all screens in this session. Just set up the system + one pilot.
Bulk migration happens session-by-session as we touch each screen.
```

**After session 3:** Test on your phone that the language picker screen still looks the same — this proves i18n is working transparently.

---

## Session 4 — Build lib/ utilities

**Goal:** Create api.ts (axios client), storage.ts (MMKV), and auth-storage helpers. Firebase native SDK deferred until Session 22.
**Estimated time:** 45-60 minutes  
**Blockers cleared after:** Screens can call backend APIs and persist state locally.

```
Session 4: Build the lib/ utilities that all future screens will use.

Rules from CLAUDE.md. No agents. One file at a time.

STEP 1: Verify what exists:
1. ls apps/mobile/lib/
2. What's in each existing file (theme.ts, i18n.ts if session 3 done)

Wait for "next".

STEP 2: Create apps/mobile/lib/storage.ts:
- Use react-native-mmkv
- Export a single MMKV instance
- Export typed helpers: 
  - getString(key), setString(key, value), removeString(key)
  - getObject<T>(key), setObject<T>(key, value)
  - getBoolean(key), setBoolean(key, value)
- Also export a constant STORAGE_KEYS object with: 
  LANGUAGE, JWT, PHONE, ONBOARDING_STATE, USER_ID, TENANT_ID
Show diff. Wait for approval.

STEP 3: Create apps/mobile/lib/api.ts:
- axios instance
- baseURL from process.env.EXPO_PUBLIC_API_URL (default http://localhost:8000/v1)
- Request interceptor: attach Authorization: Bearer <jwt> from storage if exists
- Request interceptor: attach X-Tenant-Id header (default 'kurmi' for now)
- Response interceptor: on 401, clear JWT from storage
- Export the axios instance as default
- Export helper types: ApiError, ApiSuccess<T>
Show diff. Wait for approval.

STEP 4: Create apps/mobile/lib/auth-storage.ts:
- Helpers built on storage.ts specifically for auth:
  - saveSession(jwt: string, userId: string)
  - clearSession()
  - getSession(): { jwt: string, userId: string } | null
  - isLoggedIn(): boolean
Show diff. Wait for approval.

STEP 5: Create apps/mobile/hooks/useAuth.ts:
- Simple hook that reads from auth-storage.ts
- Returns { isLoggedIn, jwt, userId, login, logout }
- login/logout methods just proxy to auth-storage.ts (no real Firebase yet)
- Uses zustand for reactivity (so components re-render on login/logout)
Show diff. Wait for approval.

STEP 6: Update apps/mobile/app/_layout.tsx:
- Import and initialize storage on app start (side-effect import)
- No visual changes
Show diff. Wait for approval.

STEP 7: Verify:
- pnpm tsc --noEmit (should be 0 errors)
- No app changes needed on phone (utilities aren't yet used)

STEP 8: Commit as: "feat(mobile): add lib/api, lib/storage, lib/auth-storage, hooks/useAuth"

DO NOT wire these into screens yet. That happens when we hook screens to backend 
in Session 9+.
```

**After session 4:** No visible change on phone. The utilities exist and are ready to be used when we build backend integration.

---

## Session 5 — FastAPI scaffold + Tenant module

**Goal:** Bootstrap the backend, set up Postgres + Redis via Docker, create the Tenant module with multi-tenancy middleware.
**Estimated time:** 90-120 minutes  
**Blockers cleared after:** You have a running FastAPI server on localhost:8000 with a health endpoint and tenant table.

```
Session 5: Scaffold the FastAPI backend and create the Tenant module.

Rules from CLAUDE.md apply. No agents. One step at a time.
Follow the folder structure and conventions defined in CLAUDE.md § "Folder structure" 
and § "Module pattern in FastAPI".

STEP 1: Show me the current state:
1. ls apps/api/
2. Does docker-compose.yml exist at repo root or elsewhere?
3. Is there a python .venv anywhere?

Wait for "next".

STEP 2: Create the folder structure:
apps/api/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py       # Pydantic settings
│   │   ├── db.py           # SQLAlchemy async engine
│   │   ├── security.py     # JWT + Firebase verify (stubs for now)
│   │   ├── tenant.py       # ContextVar, middleware, event listener
│   │   ├── logging.py      # structlog setup
│   │   └── deps.py         # common FastAPI dependencies
│   ├── modules/
│   │   ├── __init__.py
│   │   ├── health/
│   │   │   ├── __init__.py
│   │   │   └── router.py
│   │   └── tenant/
│   │       ├── __init__.py
│   │       ├── models.py
│   │       ├── schemas.py
│   │       ├── repository.py
│   │       ├── service.py
│   │       └── router.py
│   └── shared/
│       └── __init__.py
├── alembic/                # will be initialized
├── tests/
├── pyproject.toml
├── Dockerfile
├── .env.example
└── README.md

Show me the plan first. Wait for approval.

STEP 3: Create pyproject.toml with uv:
Dependencies:
- fastapi, uvicorn[standard], python-multipart
- sqlalchemy[asyncio], asyncpg, alembic
- pydantic, pydantic-settings
- python-jose[cryptography], passlib[bcrypt]
- redis, arq
- httpx, structlog
- firebase-admin (for later)
- boto3 (for R2)
- razorpay (for later)

Dev dependencies:
- pytest, pytest-asyncio, httpx (for testing), ruff, mypy

Show file. Wait for approval.

STEP 4: Run: cd apps/api && uv sync
Show output. Wait for approval.

STEP 5: Create/update docker-compose.yml at repo root (or apps/api/ if you prefer):
- postgres:16-alpine on 5432, volumes for persistence
- redis:7-alpine on 6379
- Both with restart policies
Show file. Wait for approval.

STEP 6: Run: docker compose up -d
Verify with docker ps.

STEP 7: Create app/core/config.py:
Pydantic Settings class reading .env with:
- DATABASE_URL, REDIS_URL
- JWT_SECRET, JWT_ALGORITHM, JWT_EXPIRE_MINUTES
- FIREBASE_PROJECT_ID, FIREBASE_CREDENTIALS_JSON (path to service account)
- DEFAULT_TENANT_ID (for local dev)
- ENVIRONMENT (dev/staging/prod)
- LOG_LEVEL

Also create .env.example with all keys documented but empty/sample values.

Show file. Wait for approval.

STEP 8: Create app/core/db.py:
- Async SQLAlchemy engine
- Base declarative class with UUID v7 default for id, timestamps, tenant_id 
  (all except tenants table)
- async_session context manager
- Function to get DB session as FastAPI dependency
Show file. Wait for approval.

STEP 9: Create app/core/tenant.py:
- ContextVar[str | None] for current_tenant_id
- FastAPI middleware that resolves tenant from:
  1. X-Tenant-Id header if present
  2. Subdomain if request.host contains a subdomain
  3. Default from settings if neither
- Sets ContextVar for the request duration
- SQLAlchemy event listener on 'do_orm_execute' that auto-filters SELECT queries 
  by tenant_id if the mapper has a tenant_id column
Show file. Wait for approval.

STEP 10: Create app/modules/tenant/ files:
- models.py: Tenant model (id, name, subdomain unique, app_name, logo_url, 
  primary_color, accent_color, default_language, support_phone, support_whatsapp, 
  launch_state enum, feature_flags JSONB, created_at, updated_at). No tenant_id 
  column on this table itself.
- schemas.py: TenantCreate, TenantUpdate, TenantResponse Pydantic models
- repository.py: get_by_id, get_by_subdomain, create, update, list_all
- service.py: business logic
- router.py: admin endpoints (POST /v1/admin/tenants, GET /v1/admin/tenants, 
  GET /v1/admin/tenants/{id})

Show each file. Wait for approval per file.

STEP 11: Create app/modules/health/router.py:
GET /v1/health returning {status: "ok", version: "0.1.0", timestamp: ISO8601}

STEP 12: Create app/main.py:
- FastAPI app with proper title
- Add tenant middleware
- Include health router at /v1
- Include tenant router at /v1/admin
- Configure CORS for localhost:8081 (Expo) and localhost:5173 (Vite admin)
Show file. Wait for approval.

STEP 13: Initialize Alembic:
- cd apps/api && alembic init alembic
- Configure alembic to use async engine and app.core.db.Base metadata
- Generate first migration: alembic revision --autogenerate -m "create tenants table"
- Apply: alembic upgrade head
Show migration file. Wait for approval before applying.

STEP 14: Seed a default 'kurmi' tenant:
Create a script apps/api/scripts/seed_tenant.py that inserts a default tenant with 
subdomain='kurmi', name='KurmiConnect', app_name='KurmiConnect', primary_color='#D97706', 
default_language='hi'. Run it.

STEP 15: Test:
- Start: cd apps/api && source .venv/bin/activate && uvicorn app.main:app --reload --port 8000
- Curl: curl http://localhost:8000/v1/health
- Curl: curl -H "X-Tenant-Id: kurmi" http://localhost:8000/v1/admin/tenants
- Report both outputs

STEP 16: Commit as: "feat(api): scaffold FastAPI + Tenant module with multi-tenancy"
```

**After session 5:** You have a running FastAPI server. Test with the curl commands. Should return valid JSON.

---

## Session 6 — Auth module

**Goal:** Firebase token verification + your app's own JWT issuance. Users authenticate by sending Firebase ID token to your backend, backend verifies with Firebase, returns app JWT.
**Estimated time:** 90 minutes  
**Blockers cleared after:** You can authenticate a fake user via API endpoint.

```
Session 6: Build the Auth module. Firebase verifies phone identity; our JWT 
authorizes API access.

Rules from CLAUDE.md.

STEP 1: Read PROJECT_PLAN.md § Auth module. Confirm approach:
- User authenticates via Firebase Auth on client (Session 22 will wire this)
- Client sends Firebase ID token to our POST /v1/auth/firebase-login
- We verify the token using firebase-admin SDK
- We issue our own JWT with user_id + tenant_id claims
- All subsequent requests use our JWT
- Refresh token flow for JWT renewal

Confirm. Wait for "next".

STEP 2: Set up Firebase project (I'll do this outside Claude Code):
- I'll create a Firebase project at https://console.firebase.google.com
- Enable Phone Authentication
- Generate a service account JSON key
- Save as apps/api/firebase-service-account.json (gitignored)
- Add path to apps/api/.env as FIREBASE_CREDENTIALS_JSON

When ready, tell me to proceed with step 3.

STEP 3: Update app/core/security.py:
- verify_firebase_token(token: str) -> dict — uses firebase-admin SDK to verify
- create_jwt(user_id: str, tenant_id: str) -> str
- create_refresh_token(user_id: str, tenant_id: str) -> str
- decode_jwt(token: str) -> dict (validates and returns claims)
- get_current_user() as FastAPI dependency (extracts JWT, decodes, injects user_id)
Show file. Wait for approval.

STEP 4: Create app/modules/auth module (models.py, schemas.py, etc.):
- User model: id (UUID), tenant_id, phone (E.164), firebase_uid unique, 
  role (user/moderator/editor/admin/super_admin), status (active/suspended/deleted/pending), 
  language_pref, fcm_token, last_login_at, created_at
- UserSession model: id, user_id, refresh_token_hash, expires_at, created_at, revoked_at
- Schemas: FirebaseLoginRequest (id_token), LoginResponse (access_token, refresh_token, 
  user), RefreshRequest, UserResponse
- Repository: get_by_phone, get_by_firebase_uid, create, update, get_active_sessions
- Service: firebase_login (verify token → find/create user → issue JWT + refresh), 
  refresh_access_token, logout
- Router: 
  - POST /v1/auth/firebase-login
  - POST /v1/auth/refresh
  - POST /v1/auth/logout (requires auth)
  - GET /v1/auth/me (requires auth)

Show each file, one at a time. Wait for approval per file.

STEP 5: Update Alembic:
alembic revision --autogenerate -m "create users and sessions tables"
Show migration. Wait for approval before applying.

STEP 6: Register auth router in main.py. Show diff.

STEP 7: Write tests:
- tests/test_auth.py covering:
  - firebase_login with mock Firebase (patch verify_firebase_token)
  - refresh with valid refresh_token
  - refresh with invalid token returns 401
  - /me endpoint returns current user
  - /me without token returns 401
Show file. Wait for approval.

STEP 8: Run tests:
pytest tests/test_auth.py -v
Report all pass.

STEP 9: Commit as: "feat(api): auth module with Firebase verify and JWT issuance"
```

**After session 6:** Backend can authenticate users. No mobile integration yet — that's Session 22.

---

## Session 7 — Profile module

**Goal:** All 14 onboarding fields as database columns, CRUD endpoints, photo URLs (upload deferred until R2 is set up).
**Estimated time:** 90-120 minutes  
**Blockers cleared after:** You can create and read profiles via API.

```
Session 7: Build the Profile module — the biggest module in the app.

Rules from CLAUDE.md. This module has many fields; break into logical sub-tables.

STEP 1: Read PROJECT_PLAN.md § Profile module for the field list. 
Read design/html-mockups/screens-1-20.jsx for the onboarding screens 5-20 to confirm 
which fields are collected.

Report your field inventory. Wait for approval.

STEP 2: Propose the data model:
- profiles table: main table with basics, community, location, native, education, 
  profession, lifestyle, family, horoscope, about
  Fields per CLAUDE.md § Profile module.
  Note: profile_for enum (self/son/daughter/brother/sister/relative)
- profile_photos table: id, profile_id, url_thumb, url_medium, url_full, 
  is_main, display_order, uploaded_at
- profile_preferences table: partner_age_min, partner_age_max, partner_height_min, 
  partner_height_max, partner_marital_status[], partner_castes[], partner_sub_castes[], 
  partner_educations[], partner_professions[], partner_states[], partner_diet[], 
  partner_manglik, partner_income_min
- sub_castes table (per-tenant lookup): id, tenant_id, name_hi, name_en, display_order
- gotras table (per-tenant lookup): id, tenant_id, name_hi, name_en

Show ER-diagram-like description. Wait for approval.

STEP 3: Create models.py for profiles module. Watch for:
- All FK to users(id) and tenant_id
- Use PostgreSQL ARRAY type for multi-value fields
- Use JSONB for future extensibility (hobbies_extra, etc.)
- Add relevant indexes: (tenant_id, sub_caste, district), (tenant_id, gender, age)
- Timestamps everywhere
Show file. Wait for approval.

STEP 4: Create schemas.py:
- Split into logical sections matching onboarding screens
- ProfileBasicsCreate/Update, ProfileCommunityCreate/Update, etc.
- ProfileFullResponse combining all sections
- Photo schemas
- Preference schemas
Show file. Wait for approval.

STEP 5: Create repository.py, service.py, router.py:
Endpoints:
- POST /v1/profiles (create own profile)
- GET /v1/profiles/me (get own profile)
- PATCH /v1/profiles/me/basics
- PATCH /v1/profiles/me/community
- PATCH /v1/profiles/me/location
- PATCH /v1/profiles/me/native
- PATCH /v1/profiles/me/education
- PATCH /v1/profiles/me/profession
- PATCH /v1/profiles/me/lifestyle
- PATCH /v1/profiles/me/family
- PATCH /v1/profiles/me/horoscope
- PATCH /v1/profiles/me/about
- PATCH /v1/profiles/me/preferences
- GET /v1/profiles/{id} (view another user's profile)
- GET /v1/profiles/lookups/sub-castes (tenant's sub-castes list)
- GET /v1/profiles/lookups/gotras (tenant's gotras list)
- POST /v1/profiles/photos (metadata only — URL upload comes later)
- DELETE /v1/profiles/photos/{id}

Show one at a time. Wait for approval per file.

STEP 6: Migration:
alembic revision --autogenerate -m "create profiles, photos, preferences, sub_castes, gotras"
Show migration. Wait for approval. Apply.

STEP 7: Seed the Kurmi tenant with:
- 12 sub-castes (Awadhia, Kanaujia, Sachan, Katiyar, Patel, Patidar, Kushwaha, Maurya, 
  Chandrakar, Mahato, Verma, Singh) — both Hindi and English names
- 20-30 common Kurmi gotras (Kashyap, Bhardwaj, Vatsa, Vashishtha, Sandilya, Garg, 
  Atri, Gautam, Parashar, etc.)

Create scripts/seed_kurmi_lookups.py. Run it.

STEP 8: Write tests for major endpoints. At least: create profile, get profile, 
update section, get sub-castes, tenant isolation (can't see other tenant's profiles).

STEP 9: Commit as: "feat(api): profile module with 14-section onboarding fields"
```

**After session 7:** Profile module ready. No mobile integration yet.

---

## Session 8 — Match module

**Goal:** Compute daily matches based on preferences. Match score algorithm v1.
**Estimated time:** 90 minutes  
**Blockers cleared after:** Home screen can show real "Today's matches" once wired.

```
Session 8: Build the Match module — daily match suggestions and match score.

Rules from CLAUDE.md.

STEP 1: Read PROJECT_PLAN.md § Match module. Confirm approach:
- For each user, compute a set of match candidates (opposite gender, within 
  partner preferences)
- Compute a score 0-100 based on: community fit (30%), age fit (15%), 
  location proximity (15%), education (10%), profession (10%), horoscope (10%), 
  lifestyle overlap (10%)
- Store computed matches in matches table with score
- Refresh matches daily via arq scheduled job
- API endpoint to get "Today's matches" returns top 20 for user

Confirm. Wait for approval.

STEP 2: Design the algorithm. Show me a Python pseudo-code function 
compute_match_score(user_a, user_b) -> (score: int, breakdown: dict). Wait for approval.

STEP 3: Create app/modules/match/:
- models.py: Match (user_a_id, user_b_id, match_score, score_breakdown JSONB, 
  generated_at, viewed_by_a, viewed_by_b, shortlisted_by_a, shortlisted_by_b)
- schemas.py: MatchResponse with mini-profile data
- repository.py: get_todays_matches, mark_viewed, mark_shortlisted
- service.py: compute_match_score function, generate_matches_for_user
- router.py:
  - GET /v1/matches/today
  - GET /v1/matches/shortlist
  - POST /v1/matches/{match_id}/shortlist
  - DELETE /v1/matches/{match_id}/shortlist
  - POST /v1/matches/{match_id}/view (mark viewed)

Show each file. Wait for approval.

STEP 4: Set up arq worker (background jobs):
- app/workers/main.py — arq worker config
- app/workers/tasks/generate_daily_matches.py — cron job at 5 AM IST daily
- The job iterates active users and calls service.generate_matches_for_user
Show files. Wait for approval.

STEP 5: Migration + apply.

STEP 6: Seed test data:
Create scripts/seed_test_profiles.py that creates 20-30 fake Kurmi profiles 
(mix of Awadhia, Kanaujia, etc., various districts) so we can test matching.

STEP 7: Test manually:
- Create a test user (male, 28, Lucknow, Kanaujia)
- Trigger match generation
- Curl GET /v1/matches/today
- Should return female Kurmi profiles ranked by score

STEP 8: Write tests for algorithm.

STEP 9: Commit as: "feat(api): match module with score algorithm v1"
```

**After session 8:** Backend generates matches. No mobile display yet.

---

## Session 9 — Wire onboarding screens 1-7 to backend

**Goal:** Basic → Physical → Community → Location → Native → Education → Profession screens save to the profile API.
**Estimated time:** 2-3 hours  
**Blockers cleared after:** First 7 onboarding screens actually save data.

```
Session 9: Wire mobile onboarding screens 1-7 to the Profile API.

Prerequisites:
- Session 7 (Profile module) is done
- lib/api.ts and hooks/useAuth exist (Session 4)
- BottomCTA is added to for-whom.tsx (Session 1)

Rules from CLAUDE.md. One screen at a time.

STEP 1: Review the seven screens I'll wire this session:
1. apps/mobile/app/(onboard)/for-whom.tsx (already has button, needs API save)
2. apps/mobile/app/(onboard)/basic.tsx (first name, last name, gender, dob, marital status)
3. apps/mobile/app/(onboard)/physical.tsx (height, weight, body type, complexion)
4. apps/mobile/app/(onboard)/community.tsx (sub-caste, gotra, mother tongue)
5. apps/mobile/app/(onboard)/location.tsx (state, district, city, residing since)
6. apps/mobile/app/(onboard)/native.tsx (native place details)
7. apps/mobile/app/(onboard)/education.tsx (qualification, specialization, college)

Read all 7 files. Report:
- Current state (fields present, validation exists?, button wired?)
- Field mismatches vs backend Profile schema

Wait for "next".

STEP 2: Create shared onboarding state management.
apps/mobile/hooks/useOnboarding.ts:
- Zustand store holding partial profile data across screens
- Actions: updateBasics, updatePhysical, updateCommunity, ...etc
- Persist to MMKV so user can resume after app close
- getCurrentStep(), goToNextStep(), completeOnboarding()
Show file. Wait for approval.

STEP 3: For EACH screen (do them one at a time, approval between each):
- Ensure react-hook-form + Zod validation
- Use useTranslation for all strings (add missing keys to hi.json/en.json)
- BottomCTA at bottom:
  - Save current section via appropriate PATCH endpoint
  - On success, router.push to next screen
  - On error, show inline error message
- Add loading state during save
- Add proper keyboard types (numeric for phone/age, etc.)
- Preserve visual design exactly

Order: for-whom → basic → physical → community → location → native → education

STEP 4: After each screen, we test end-to-end from phone:
- I complete the section on my phone
- Backend should have the data (check via curl or SQL)
- Report success before moving to next screen

STEP 5: Sub-caste and gotra dropdowns on community.tsx:
- Fetch from GET /v1/profiles/lookups/sub-castes on mount
- Cache in TanStack Query
- Show as searchable dropdown/picker

STEP 6: State/district cascading dropdowns on location.tsx:
- Use a static JSON file of Indian states/districts (add apps/mobile/data/india.json)
- OR fetch from a backend endpoint (deferred)

STEP 7: Commit at each stable point:
"feat(mobile): wire onboard/basic to API"
"feat(mobile): wire onboard/community with sub-caste/gotra dropdowns"
etc.
```

**After session 9:** First 7 onboarding screens fully functional end-to-end.

---

## Session 10 — Wire onboarding screens 8-14 + photo upload

**Goal:** Profession → Lifestyle → Family → Horoscope → About → Photos → Preferences all save.
**Estimated time:** 2-3 hours  
**Blockers cleared after:** Complete onboarding possible on phone with real data persistence.

```
Session 10: Wire the remaining 7 onboarding screens + set up photo upload to R2.

Prerequisites: Session 9 done. Cloudflare R2 bucket exists (I'll set this up).

STEP 1: Verify prerequisites:
- Session 9 screens save correctly
- R2 credentials in apps/api/.env: R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, 
  R2_BUCKET_NAME, R2_PUBLIC_URL

If R2 not set up yet, stop and set that up first. I'll do it via Cloudflare dashboard.

STEP 2: Backend — add photo upload endpoint:
- POST /v1/profiles/photos/upload
- Accepts multipart file
- Server-side: resize via Sharp/Pillow into thumb (200px), medium (600px), full (1200px)
- Upload all three to R2 with unique keys: {tenant_id}/profiles/{user_id}/{uuid}_full.jpg
- Return { url_thumb, url_medium, url_full }
- Add to profile_photos table
Show file. Wait for approval.

STEP 3: Wire mobile screens one at a time (approval between each):
- (onboard)/profession.tsx
- (onboard)/lifestyle.tsx  
- (onboard)/family.tsx (complex — many fields)
- (onboard)/horoscope.tsx
- (onboard)/about.tsx (text area)
- (onboard)/photos.tsx — this one is special:
  - 6 photo slots
  - expo-image-picker for camera/gallery
  - Compress locally before upload (already compressed by backend, but reduce network)
  - Upload each photo to /v1/profiles/photos/upload
  - Show upload progress per slot
  - Handle upload errors gracefully
- (onboard)/prefs-basic.tsx
- (onboard)/prefs-community.tsx
- (onboard)/prefs-career.tsx

STEP 4: Onboarding completion:
Last screen: submit final profile, mark completed, redirect to (tabs)/index (home).

STEP 5: Commit as: "feat: complete onboarding flow end-to-end with photo upload"
```

**After session 10:** Full onboarding works. You can complete a profile on your phone, and it saves to the database.

---

## Session 11 — Wire Home + Search + Profile detail

**Goal:** After onboarding, user lands on home screen showing real "Today's matches" from backend.
**Estimated time:** 2 hours  
**Blockers cleared after:** Real matches display, users can search, view profile detail.

```
Session 11: Wire the home screen, search, and profile detail to backend match/profile APIs.

STEP 1: Review the three screens:
1. apps/mobile/app/(tabs)/index.tsx (Home — Today's matches)
2. apps/mobile/app/(tabs)/search.tsx (Search entry)
3. apps/mobile/app/search/filters.tsx (Filters)
4. apps/mobile/app/search/results.tsx (Results)
5. apps/mobile/app/profile/[id].tsx (Profile detail)

Report current state of each. Wait for approval.

STEP 2: Backend — add search endpoint:
GET /v1/profiles/search with query params:
- sub_castes[], gotras[], states[], districts[]
- age_min, age_max
- height_min, height_max
- marital_status[]
- educations[], professions[]
- has_photo, is_verified, is_featured
Returns paginated list with mini-profile data.

Show file. Wait for approval.

STEP 3: Wire (tabs)/index.tsx:
- On mount, fetch /v1/matches/today via TanStack Query
- Display 2-column grid of MatchCard components
- Fetch /v1/featured (deferred if Featured module not done)
- Pull-to-refresh
- Empty state, error state, loading state

STEP 4: Wire (tabs)/search.tsx + filters.tsx + results.tsx:
- filters.tsx: filter form with all criteria
- On apply, navigate to results.tsx with filters as params
- results.tsx: infinite scroll via TanStack Query useInfiniteQuery
- Save recent searches to MMKV

STEP 5: Wire profile/[id].tsx:
- Fetch /v1/profiles/{id} on mount
- Show all sections
- Send Interest, Shortlist, Share buttons (Interest wiring in Session 12)
- Share generates a WhatsApp-shareable text

STEP 6: Commit progressively.
```

**After session 11:** Real match data flows to home. Search works. Profile detail loads.

---

## Session 12 — Interest module

**Goal:** Send/accept/decline interests. Mutual match creates chat room.
**Estimated time:** 90 minutes

```
Session 12: Build Interest module (backend + wire mobile screens).

STEP 1: Backend — app/modules/interest/:
- models.py: Interest (sender_id, receiver_id, status: sent/accepted/declined/withdrawn, 
  sent_at, responded_at, decline_reason?)
- schemas.py, repository.py, service.py, router.py
- Endpoints:
  - POST /v1/interests (send)
  - GET /v1/interests/sent
  - GET /v1/interests/received
  - GET /v1/interests/mutual
  - POST /v1/interests/{id}/accept
  - POST /v1/interests/{id}/decline
  - DELETE /v1/interests/{id} (withdraw)
- On accept: create ChatRoom (defer chat implementation to Session 13)

Show one at a time. Wait for approval.

STEP 2: Migration + apply.

STEP 3: Wire mobile screens:
- profile/send-interest.tsx (confirmation modal)
- (tabs)/interests.tsx (three tabs: sent/received/accepted)
- interests/received.tsx
- interests/accepted.tsx
- shortlist.tsx (was there before, wire it to /v1/matches/shortlist)

STEP 4: Update profile/[id].tsx Send Interest button to call POST /v1/interests.

STEP 5: Commit.
```

**After session 12:** Users can send interests, receive them, accept/decline. Mutual matches are trackable.

---

## Session 13 — Chat module (WebSocket + mobile chat screen)

**Goal:** Real-time chat between mutually matched users.
**Estimated time:** 2-3 hours

```
Session 13: Build the Chat module. FastAPI WebSocket for real-time, Redis for pub/sub 
to support multiple workers later.

STEP 1: Backend — app/modules/chat/:
- models.py: ChatRoom (id, user_a_id, user_b_id, tenant_id, created_at, last_msg_at)
  Message (id, room_id, sender_id, content, msg_type: text/image/system, sent_at, 
  read_at, flagged: bool, flag_reason?)
- schemas.py: MessageCreate, MessageResponse, ChatRoomResponse (with last message)
- WebSocket endpoint: /v1/ws/chat?token=<jwt>
  - Verify JWT
  - Connect user to a Redis pub/sub channel: user:{user_id}
  - When user sends message: save to DB, publish to receiver's channel
  - When receiving publish for own channel: send WebSocket message to client
- REST endpoints:
  - GET /v1/chat/rooms (list of user's chat rooms)
  - GET /v1/chat/rooms/{room_id}/messages (paginated history)
  - POST /v1/chat/rooms/{room_id}/messages (send — for offline fallback if WS closed)
  - PATCH /v1/chat/rooms/{room_id}/messages/{msg_id}/read

- Content safety: detect phone numbers, UPI IDs, WhatsApp mentions. 
  If detected, flag the message and warn users (don't block, just add a system message).

Show files one at a time. Wait for approval each.

STEP 2: Migration + apply.

STEP 3: Wire mobile:
- (tabs)/chats.tsx — list of chat rooms (fetch /v1/chat/rooms)
- chat/[id].tsx — the actual chat conversation:
  - Connect WebSocket on mount, disconnect on unmount
  - Show messages (react-native-gifted-chat or custom)
  - Send message via WebSocket
  - Fallback to REST if WebSocket disconnected
  - Show typing indicator (via WebSocket events)
  - Auto-scroll to bottom on new message
  - Load older messages on scroll up

STEP 4: Commit.
```

**After session 13:** Users can chat in real-time after mutual match.

---

## Session 14 — Subscription + Razorpay

**Goal:** Users can buy subscription plans and contact packs.
**Estimated time:** 2-3 hours

```
Session 14: Subscription + Payment via Razorpay.

Prerequisites: Razorpay business account set up, KYC done. Test mode keys in .env.

STEP 1: Read PROJECT_PLAN.md § Revenue streams for pricing:
- Contact packs: ₹99/3, ₹249/10, ₹499/25
- Subscriptions: Silver ₹599/3mo, Gold ₹1499/6mo, Platinum ₹3499/12mo
- Add-ons: 24h boost ₹99, Kundli ₹199, biodata ₹199, video ₹149

STEP 2: Backend modules:
- app/modules/plan/: Plans (fixed catalog), read-only for now
  - GET /v1/plans (list)
  - Seed 3 subscription plans + 3 contact packs + add-ons
  
- app/modules/subscription/: 
  - Subscription (user_id, plan_id, status, started_at, expires_at, contacts_used, 
    boosts_used, auto_renew, razorpay_subscription_id)
  - Endpoints: GET /v1/subscriptions/me, POST /v1/subscriptions (create with plan_id 
    triggers Razorpay order)

- app/modules/payment/:
  - Payment (user_id, amount_paise, currency, gateway, gateway_txn_id, status, 
    item_type: subscription/pack/addon, item_id, created_at)
  - Endpoints:
    - POST /v1/payments/order (create Razorpay order for a plan/pack/addon)
    - POST /v1/payments/verify (verify Razorpay signature, mark payment success)
    - POST /v1/payments/webhook (Razorpay webhook — verify signature, update status)
    - POST /v1/payments/{id}/refund (admin only)
  - Store all money in paise (integer). ₹599 = 59900.

Show each module one at a time. Wait for approval.

STEP 3: Migration + apply.

STEP 4: Wire mobile:
- plans/index.tsx — fetch and display plans + packs + addons
- plans/checkout.tsx — call POST /v1/payments/order, get Razorpay order, open 
  Razorpay checkout via react-native-razorpay package
  - npx expo install react-native-razorpay
- plans/success.tsx, plans/failure.tsx
- subscription.tsx (My Subscription) — fetch /v1/subscriptions/me
- plans/addons.tsx

STEP 5: Test with Razorpay test mode. Report end-to-end payment flow.

STEP 6: Commit.
```

**After session 14:** Users can pay for plans on your phone (in Razorpay test mode).

---

## Session 15 — Verification module

**Goal:** Aadhaar, education, employer verification workflows.
**Estimated time:** 90 minutes

```
Session 15: Verification module. Users upload documents; admin approves.

STEP 1: Backend:
- Verification model: id, user_id, type: aadhaar/education/employer/income/photo, 
  document_url (R2), status: pending/approved/rejected, reviewed_by, reviewed_at, 
  rejection_reason
- Endpoints:
  - POST /v1/verifications (user submits — uploads document to R2, creates row)
  - GET /v1/verifications/me (user's status per type)
  - GET /v1/admin/verifications?status=pending (admin queue — filtered)
  - POST /v1/admin/verifications/{id}/approve
  - POST /v1/admin/verifications/{id}/reject
- On approve: set corresponding badge on profile
  
Show. Wait for approval.

STEP 2: Migration + apply.

STEP 3: Wire mobile:
- verify/index.tsx (verification center — status per type)
- verify/document.tsx (upload flow per type)

STEP 4: Aadhaar via DigiLocker (deferred — for now just upload PDF/image).

STEP 5: Commit.
```

**After session 15:** Users submit verification docs. Admin approves later.

---

## Session 16 — Featured Personality backend

**Goal:** Editorial workflow for Featured Personalities (your USP).
**Estimated time:** 90 minutes

```
Session 16: Featured Personality module. Editorial curation, not paid placement.

STEP 1: Backend:
- FeaturedCategory (per-tenant lookup): civil_services, doctors, engineers, academia, 
  business, agriculture, sports_arts, social_work
- FeaturedPersonality: id, user_id, category_id, title_line (headline like 
  "IAS 2019 • Joint Magistrate, Sitapur"), editorial_blurb (≤200 chars), 
  supporting_docs[], nomination_source (self/community/editorial), 
  approval_status: draft/under_review/approved/rejected/published/revoked/expired, 
  featured_since, expires_at, display_priority, admin_notes
- Endpoints (public):
  - GET /v1/featured?category=xxx (list published)
  - GET /v1/featured/{id}
- Endpoints (admin):
  - POST /v1/admin/featured/nominations
  - GET /v1/admin/featured/nominations?status=under_review
  - POST /v1/admin/featured/{id}/approve
  - POST /v1/admin/featured/{id}/reject
  - POST /v1/admin/featured/{id}/publish
  - POST /v1/admin/featured/{id}/revoke

Show. Wait for approval per file.

STEP 2: Migration + apply. Seed the 8 categories.

STEP 3: Commit.

STEP 4: Wire mobile screens:
- featured/index.tsx (landing with category filter chips)
- featured/[category].tsx (filtered by category)
- featured/[id].tsx (detail with editorial blurb)

STEP 5: Home carousel — update to fetch /v1/featured (limit 6).
```

**After session 16:** Featured Personality data available. Editorial workflow accessible via admin API (admin UI in Session 20).

---

## Session 17 — Wire Featured screens on mobile

**Goal:** Featured Personalities visible in app.
**Estimated time:** 60-90 minutes

```
Session 17: Wire mobile Featured screens and add to Home carousel.

STEP 1: Review current featured/*.tsx files. Report state.

STEP 2: Wire featured/index.tsx:
- Fetch /v1/featured with all categories
- Category filter chips at top
- List of featured cards with photo + name + credential pill + editorial blurb 
  (for top-tier)
- Load more on scroll

STEP 3: Wire featured/[category].tsx: similar but filtered.

STEP 4: Wire featured/[id].tsx: distinctive layout per wireframe:
- Amber "Featured Personality • Category" strip at top
- Credential pill below name
- Italic editorial blurb with orange left border
- Verified credentials grid

STEP 5: Update (tabs)/index.tsx to show Featured carousel.

STEP 6: Commit.
```

---

## Session 18 — Admin scaffold + Login + Dashboard

**Goal:** Start on the admin panel. Login + Dashboard KPIs.
**Estimated time:** 2 hours

```
Session 18: Scaffold admin panel and build first 2 screens.

STEP 1: Review current apps/admin/ state.

STEP 2: Set up:
- Vite + React 19 + TS
- shadcn/ui init with your amber theme  
- TanStack Router + TanStack Query + TanStack Table
- axios client at src/lib/api.ts (talks to same backend)
- Zod schemas

STEP 3: Build src/routes/login.tsx:
- Email + password form
- POST /v1/admin/auth/login (need backend endpoint — add if missing)
- Store admin JWT
- Redirect to /dashboard

STEP 4: Build src/routes/dashboard.tsx:
- KPIs: total profiles, paid users, revenue MTD, DAU
- Fetch from GET /v1/admin/stats (need backend endpoint)
- Queue counts: verifications, featured, moderation, reports

STEP 5: Build src/components/AdminShell.tsx: sidebar nav + header + main content area.

STEP 6: Commit.
```

---

## Session 19 — Admin: User management + Moderation

**Goal:** Admin can view users, ban, moderate photos, review reports.
**Estimated time:** 2 hours

```
Session 19: Admin user management + moderation queues.

STEP 1: Wire src/routes/users/index.tsx: TanStack Table with all users, search, filter, 
paginate.

STEP 2: Wire src/routes/users/[id].tsx: user detail with tabs (profile, activity, 
payments, verifications, reports, notes).

STEP 3: Wire src/routes/moderation.tsx: photo moderation queue + text moderation.

STEP 4: Wire src/routes/reports.tsx: user reports queue.

STEP 5: Wire src/routes/verifications.tsx: verification approval queue with document 
viewer.

STEP 6: Commit.
```

---

## Session 20 — Admin: Featured Personality editorial workflow

**Goal:** Admin can nominate, review, publish Featured Personalities.
**Estimated time:** 90 min

```
Session 20: Featured Personality admin workflow. This is your USP — build it carefully.

STEP 1: Wire src/routes/featured/nominations.tsx: queue with status tabs.

STEP 2: Wire src/routes/featured/[id]/edit.tsx: editorial composer with side-by-side 
profile view and editing form.
Fields: category, title_line, editorial_blurb, supporting docs preview, publish/reject.

STEP 3: Wire src/routes/featured/categories.tsx: manage categories.

STEP 4: Wire src/routes/featured/published.tsx: view/edit published featured.

STEP 5: Commit.
```

---

## Session 21 — Admin: Payments + Broadcasts + CMS

**Goal:** Remaining admin screens.
**Estimated time:** 2 hours

```
Session 21: Admin panel remaining screens.

- src/routes/payments.tsx (transaction list, refund flow)
- src/routes/plans.tsx (plans management)
- src/routes/coupons.tsx (coupon CRUD)
- src/routes/broadcasts.tsx (push/SMS/WhatsApp composer)
- src/routes/success-stories.tsx (CMS)
- src/routes/banners.tsx (home banners)
- src/routes/tenant-config.tsx (SaaS foundation — sub-caste, gotra, feature flags)
- src/routes/admins.tsx (admin user management)

For each: standard TanStack Table + form pattern. Work through them systematically.
```

---

## Session 22 — Real Firebase OTP (dev build via EAS)

**Goal:** Replace mock OTP with real Firebase phone authentication.
**Estimated time:** 3-4 hours (mostly setup, EAS build wait time)

```
Session 22: Wire real Firebase OTP. Requires EAS custom dev build (Expo Go doesn't 
support Firebase native modules).

Prerequisites:
- Firebase project already created (should be done for backend Session 6)
- google-services.json downloaded and placed in apps/mobile/
- Phone number added to Firebase Auth test phone numbers list

STEP 1: Install:
- npx expo install @react-native-firebase/app @react-native-firebase/auth
- Update app.json to reference google-services.json

STEP 2: Install EAS CLI globally:
- pnpm add -g eas-cli
- eas login

STEP 3: Configure eas.json for a development build.

STEP 4: Build:
- eas build --profile development --platform android
- Wait 15-30 min for build to complete

STEP 5: Install the .apk on your phone (via download link from EAS).

STEP 6: Wire the phone.tsx and otp.tsx screens to real Firebase:
- signInWithPhoneNumber returns confirmation
- OTP entry calls confirmation.confirm(code)
- Get Firebase ID token
- Send to backend POST /v1/auth/firebase-login
- Store returned JWT
- Navigate to onboarding

STEP 7: Test end-to-end with real SMS to your phone.

STEP 8: Commit.
```

**After session 22:** Real OTP works. Users can log in with actual phone verification.

---

## Session 23 — Deploy backend to Hetzner + Cloudflare

**Goal:** Backend running publicly at api.kurmiconnect.in (or similar). Mobile app can talk to it from anywhere.
**Estimated time:** 3-4 hours

```
Session 23: Deploy backend to Hetzner CX22 with Docker Compose + Cloudflare.

Prerequisites:
- Hetzner account, CX22 VPS provisioned  
- Domain purchased and pointed to Cloudflare DNS
- Cloudflare account with proxied A record for api.kurmiconnect.in → Hetzner IP

STEP 1: SSH into Hetzner. Set up:
- Ubuntu 24.04 fresh install
- Docker + Docker Compose
- ufw firewall (allow 80, 443, 22 only)
- Add non-root user with sudo

STEP 2: Set up production Docker Compose:
- FastAPI app
- Postgres 16 with volume
- Redis 7
- Meilisearch (deferred, add later)  
- Nginx reverse proxy with Let's Encrypt

STEP 3: Environment variables:
- Production JWT_SECRET (rotate from dev)
- Real Firebase service account
- Real Razorpay production keys
- Real R2 credentials
- MSG91 credentials

STEP 4: GitHub Actions CI/CD:
- On push to main branch: build Docker image, push to registry, SSH deploy to Hetzner

STEP 5: Cloudflare setup:
- Proxy status: on (orange cloud)
- SSL/TLS: Full (strict)
- Rate limiting for /v1/auth/firebase-login (10 req/min per IP)

STEP 6: Point mobile app to production API URL:
- apps/mobile/.env: EXPO_PUBLIC_API_URL=https://api.kurmiconnect.in/v1
- Rebuild via EAS

STEP 7: End-to-end test from real phone → real backend → real DB.

STEP 8: Set up daily Postgres backups to Hetzner Storage Box.

STEP 9: Commit deployment configs.
```

**After session 23:** Backend runs on production infrastructure. Mobile app can call it from anywhere.

---

## Session 24 — End-to-end smoke test + fix list

**Goal:** Walk through the entire flow as a real user. List every bug. Fix critical ones.
**Estimated time:** 2-4 hours (mostly manual testing + fix time)

```
Session 24: Full user journey QA + critical bug fixes.

STEP 1: Test flow as a new user:
1. Install app fresh
2. Splash → Language → Phone → OTP (real Firebase)
3. All 14 onboarding screens
4. Upload real photos
5. Land on home
6. Browse today's matches
7. Search
8. Open a profile detail  
9. Send an interest
10. From other test account: accept the interest
11. Open chat
12. Send messages both ways
13. Browse Featured Personalities
14. Try to buy Gold plan via Razorpay test mode
15. Access admin panel from browser
16. Verify a document
17. Approve a Featured Personality nomination

For each step, record:
- ✅ Works as expected
- ⚠️  Minor issue (note it)
- ❌ Broken (fix must happen)

STEP 2: Prioritize the bug list:
- Critical: blocks core flow
- Major: broken feature but not core
- Minor: polish

STEP 3: Fix critical bugs one by one. Test after each fix.

STEP 4: Update PROJECT_PLAN.md status. Commit.

STEP 5: Tag as v0.1.0-mvp-complete.
```

**After session 24:** You have an end-to-end working MVP. Ready for soft launch to 50-100 test users.

---

## Deferred to post-MVP (Phase 2)

These are important but not blockers for a soft launch:

- Public marketing site (Next.js at kurmiconnect.in)
- SEO landing pages (per district, per sub-caste)
- Success stories submission flow (users share their story)
- Referral system with wallet
- WhatsApp biodata PDF generator
- Kundli match report generation
- Astrologer-assisted Kundli (marketplace)
- Video introduction calls
- RM-assisted matchmaking
- Wedding services marketplace
- Kurmi Setu community services track (see PROJECT_PLAN.md)
- iOS app (Android first)
- Multi-tenant SaaS expansion to other communities
- Advanced AI matching
- Photo AI verification (Google Vision integration)
- Duplicate profile detection

Don't touch these until 100+ real users are on the app and paying. Focus wins.

---

## How to know when you're done

Session 24 completes. You:
- Complete the full flow on your phone → works
- 3 friends complete the flow → works
- 1 friend actually pays for Gold in Razorpay test mode → works
- Admin can moderate everything from browser → works

That's MVP-complete. From here:

**Week +1:** Onboard 50 real Kurmi families via WhatsApp groups + community channels.
**Week +2:** Fix the top 10 bugs those users report.
**Week +3:** Publish first 5 Featured Personalities (real doctors, IAS officers you know).
**Week +4:** Convert Razorpay to production keys. First real ₹599 subscription.

That's your public launch.

---

## Rules for using this document

1. **One session at a time.** Never paste two session prompts in one Claude Code message.
2. **Follow the numbered steps within a session.** Wait for approval between steps.
3. **No agents.** These prompts explicitly forbid agents; keep it that way.
4. **Test on phone/browser after each session.** Don't move to next until current works.
5. **Commit at the end of every session** with the exact commit message specified.
6. **Update the checkboxes above** as you complete sessions.
7. **If a session takes more than 2× the estimate**, stop and reassess. Something is wrong.
8. **Weekends off.** Sustainable pace > sprint pace.
9. **Come back to Claude.ai (main chat) when stuck.** Paste the error, screenshot the phone.

---

## The pacing you should follow

- Sessions 1-4 (frontend foundation): **This week**
- Sessions 5-8 (backend foundation): **Next week**  
- Sessions 9-13 (wire mobile + backend + comms): **Week 3**
- Sessions 14-17 (monetization + Featured): **Week 4**
- Sessions 18-21 (admin panel): **Week 5**
- Sessions 22-24 (real Firebase, deploy, QA): **Week 6**

That's **6 weeks to MVP**. Aggressive but doable at 1 session per weekday evening, weekends off.
